require 'spec_helper'

describe Omniauth::Fedena do
  it 'has a version number' do
    expect(Omniauth::Fedena::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(false)
  end
end
